[package]
name = "python-cleaner"
version = "0.1.0"
edition = "2021"
authors = ["Senam Dayie"]
description = "A fast Python whitespace and indentation cleaner for syntax, git, and file size issues"
license = "MIT"
repository = "https://github.com/yourname/python-cleaner"
keywords = ["python", "formatter", "cli", "whitespace", "git"]
categories = ["command-line-utilities"]

[[bin]]
name = "python-cleaner"
path = "src/main.rs"

[dependencies]
